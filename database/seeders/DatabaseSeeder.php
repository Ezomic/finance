<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

class DatabaseSeeder extends Seeder
{
    public function run(): void
    {
        // Create your first user and household from the registration screen at /register.
        // No demo data is seeded by default — this is a real ledger, not a sandbox.
    }
}
